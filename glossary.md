# Glossary - Blame Karthik if it's wrong
Packet: small chunk of data
Network protocol layers: add shit to make understanding the data recieved easier i.e. sender info  
> PHY: physical layer

TCP: A protocol of how data transmitted from client to server - no packets are lost
IP address: looks like number.number.number.number  
Local IP address: 
Public facing IP address:
MAC address (Media Access Control): actual device's "hardcoded" address
Static IP: IP address that doesn't change (duh). Sometimes other IP addresses change to match the number of ppl using that address  
Netmask: Something that ANDs the IP address to tell that defines the sub networks. i.e. 255.255.255.0 means I have 4 bytes (from the 0) that I can allocate to my devices -  256 devices
